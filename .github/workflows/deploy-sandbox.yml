name: Deploy sandbox to GitHub Pages

on:
  push:
    branches:
      - sandbox
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-pages-reusable.yml
    permissions:
      contents: read
      pages: write
      id-token: write

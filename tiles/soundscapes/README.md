# Soundscapes tile

The Soundscapes tile keeps the legacy internal `boombox` type ID so existing saved boards remain compatible. Its playback logic, styles, and audio assets live in this folder.

The compact player is the default appearance. Vinyl, Music Player, and iPod are Tile Skins handled by the shared TeacherTiles skin/shop system.
